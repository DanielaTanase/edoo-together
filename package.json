{
  "name": "capstone-project-template",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "sass --watch src/sass:dist/css & esbuild ./src/js/main.js --bundle --outfile=./dist/js/main.bundle.js --watch & cd ./dist && live-server --port=7000 --ignore=css/sass,css/style.css.map",
    "compile": "sass src/sass:dist/css --style=compressed & esbuild ./src/js/main.js --bundle --minify --outfile=./dist/js/main.bundle.js",
    "watch": "sass --watch src/sass:dist/css",
    "build": "esbuild ./src/js/main.js --bundle --outfile=./dist/js/main.bundle.js --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://autocode.git.epam.com/tanase.ana.daniela/capstone-project-template.git"
  },
  "author": "Daniela Tanase",
  "license": "ISC",
  "dependencies": {
    "live-server": "^1.2.2",
    "sass": "1.79"
  },
  "devDependencies": {
    "esbuild": "0.25.2"
  }
}
